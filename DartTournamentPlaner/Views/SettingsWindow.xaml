<Window x:Class="DartTournamentPlaner.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Einstellungen" Height="650" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="650" MinWidth="550"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextFormattingMode="Display"
        RenderOptions.BitmapScalingMode="HighQuality">
    <Window.Resources>
        <!-- Moderne Button-Styles basierend auf dem Dialog-System -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#3B82F6" Offset="0"/>
                        <GradientStop Color="#2563EB" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}"
                                UseLayoutRounding="True" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              UseLayoutRounding="True" SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#2563EB" Offset="0"/>
                                            <GradientStop Color="#1D4ED8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#1D4ED8" Offset="0"/>
                                            <GradientStop Color="#1E3A8A" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCancelButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}"
                                UseLayoutRounding="True" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              UseLayoutRounding="True" SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Moderne ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                UseLayoutRounding="True" SnapsToDevicePixels="True">
                            <Grid>
                                <ToggleButton Name="toggleButton" 
                                              Background="Transparent" 
                                              BorderThickness="0"
                                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="20"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Name="contentPresenter" 
                                                          Grid.Column="0"
                                                          Margin="{TemplateBinding Padding}"
                                                          HorizontalAlignment="Left" 
                                                          VerticalAlignment="Center"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          UseLayoutRounding="True" SnapsToDevicePixels="True"/>
                                        <Path Grid.Column="1" Name="arrow" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z" 
                                              Fill="#6B7280"
                                              UseLayoutRounding="True" SnapsToDevicePixels="True"/>
                                    </Grid>
                                </ToggleButton>
                                <Popup Name="popup" 
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" 
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Border Background="White" 
                                            BorderBrush="#E5E7EB" 
                                            BorderThickness="1" 
                                            CornerRadius="8"
                                            UseLayoutRounding="True" SnapsToDevicePixels="True">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" 
                                                              BlurRadius="12" Opacity="0.15"/>
                                        </Border.Effect>
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Moderne CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border Name="checkBorder" 
                                    Width="20" Height="20" 
                                    Background="White" 
                                    BorderBrush="#CBD5E1" 
                                    BorderThickness="1" 
                                    CornerRadius="4"
                                    VerticalAlignment="Center"
                                    UseLayoutRounding="True" SnapsToDevicePixels="True">
                                <Path Name="checkMark" 
                                      Data="M 2 6 L 6 10 L 14 2" 
                                      Stroke="#3B82F6" 
                                      StrokeThickness="2" 
                                      Visibility="Collapsed"
                                      UseLayoutRounding="True" SnapsToDevicePixels="True"/>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                              UseLayoutRounding="True" SnapsToDevicePixels="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkBorder" Property="Background" Value="#EFF6FF"/>
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Moderne TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="2"
                                UseLayoutRounding="True" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#3B82F6" BlurRadius="8" 
                                                          Direction="0" ShadowDepth="0" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Baseline Style für alle TextBlocks -->
        <Style TargetType="TextBlock">
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>
    </Window.Resources>
    
    <!-- Hauptcontainer mit modernem Design -->
    <Border Background="Transparent" Margin="10" UseLayoutRounding="True" SnapsToDevicePixels="True">
        <Border Background="{x:Static SystemColors.WindowBrush}" 
                CornerRadius="12" 
                BorderBrush="#E2E8F0" 
                BorderThickness="1"
                UseLayoutRounding="True" SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="8" 
                                  BlurRadius="20" Opacity="0.15"/>
            </Border.Effect>
            
            <Grid UseLayoutRounding="True" SnapsToDevicePixels="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header mit Gradient -->
                <Border Grid.Row="0" CornerRadius="12,12,0,0" 
                        Padding="24,18,24,18" MouseLeftButtonDown="Header_MouseLeftButtonDown"
                        Background="{DynamicResource DialogPrimaryGradient}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x2699;" FontSize="20" Margin="0,0,12,0" 
                                   VerticalAlignment="Center"/>
                        <TextBlock Name="HeaderTitle" Text="Einstellungen" 
                                   FontSize="18" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- Content Area -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24,24,24,0">
                    <StackPanel>
                        <!-- Language Settings -->
                        <Border Background="#F9FAFB" BorderBrush="#E5E7EB" 
                                BorderThickness="1" CornerRadius="12" 
                                Padding="20" Margin="0,0,0,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="&#x1F30E;" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Name="LanguageSettingsLabel" Text="Sprache &amp; Anzeige" FontWeight="SemiBold" FontSize="15" 
                                               Foreground="#374151" VerticalAlignment="Center"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Name="LanguageLabel" Text="Sprache:" 
                                           VerticalAlignment="Center" FontWeight="Medium" 
                                           Foreground="#6B7280"/>
                                <ComboBox Grid.Row="2" Grid.Column="1" Name="LanguageComboBox" 
                                          SelectedValuePath="Tag" Margin="8,0,0,0">
                                    <ComboBoxItem Content="&#x1F1E9;&#x1F1EA; Deutsch" Tag="de"/>
                                    <ComboBoxItem Content="&#x1F1FA;&#x1F1F8; English" Tag="en"/>
                                </ComboBox>
                            </Grid>
                        </Border>
                        
                        <!-- Theme Settings -->
                        <Border Background="#F9FAFB" BorderBrush="#E5E7EB" 
                                BorderThickness="1" CornerRadius="12" 
                                Padding="20" Margin="0,0,0,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="&#x1F3A8;" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Name="ThemeSettingsLabel" Text="Design" FontWeight="SemiBold" FontSize="15" 
                                               Foreground="#374151" VerticalAlignment="Center"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Name="ThemeLabel" Text="Thema:" 
                                           VerticalAlignment="Center" FontWeight="Medium" 
                                           Foreground="#6B7280"/>
                                <ComboBox Grid.Row="2" Grid.Column="1" Name="ThemeComboBox" 
                                          SelectedValuePath="Tag" Margin="8,0,0,0">
                                    <ComboBoxItem Content="☀️ Heller Modus" Tag="Light"/>
                                    <ComboBoxItem Content="🌙 Dunkler Modus" Tag="Dark"/>
                                </ComboBox>
                            </Grid>
                        </Border>
                        
                        <!-- Auto Save Settings -->
                        <Border Background="#F9FAFB" BorderBrush="#E5E7EB" 
                                BorderThickness="1" CornerRadius="12" 
                                Padding="20" Margin="0,0,0,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="&#x1F4BE;" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Name="AutoSaveSettingsLabel" Text="Automatisches Speichern" FontWeight="SemiBold" FontSize="15" 
                                               Foreground="#374151" VerticalAlignment="Center"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Name="AutoSaveLabel" Text="Auto-Speichern:" 
                                           VerticalAlignment="Center" FontWeight="Medium" 
                                           Foreground="#6B7280"/>
                                <CheckBox Grid.Row="2" Grid.Column="1" Name="AutoSaveCheckBox" 
                                          Content="Automatisches Speichern aktivieren" 
                                          Margin="8,0,0,0"/>
                                
                                <TextBlock Grid.Row="4" Grid.Column="0" Name="IntervalLabel" Text="Intervall:" 
                                           VerticalAlignment="Center" FontWeight="Medium" 
                                           Foreground="#6B7280"/>
                                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                                    <TextBox Name="AutoSaveIntervalTextBox" Width="80" Text="5" 
                                             TextAlignment="Center"/>
                                    <TextBlock Name="IntervalUnitLabel" Text="Minuten" VerticalAlignment="Center" 
                                               Margin="12,0,0,0" Foreground="#6B7280"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Button Panel -->
                <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" 
                        Background="#FAFAFA" CornerRadius="0,0,12,12">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" 
                                Margin="24,16,24,16">
                        <Button Name="CancelButton" Content="Abbrechen" 
                                Style="{StaticResource ModernCancelButton}"
                                Click="CancelButton_Click" 
                                Margin="0,0,12,0" IsCancel="True"/>
                        <Button Name="SaveButton" Content="Speichern" 
                                Style="{StaticResource ModernButton}"
                                Click="SaveButton_Click" IsDefault="True"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>