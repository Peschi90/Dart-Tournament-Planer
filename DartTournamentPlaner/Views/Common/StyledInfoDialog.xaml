<Window x:Class="DartTournamentPlaner.Views.Common.StyledInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Info" Height="260" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">
    <Window.Resources>
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#C7D2FE" Offset="0"/>
            <GradientStop Color="#A5F3FC" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SuccessGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#A7F3D0" Offset="0"/>
            <GradientStop Color="#6EE7B7" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="ErrorGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FECACA" Offset="0"/>
            <GradientStop Color="#FCA5A5" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="OkGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#93C5FD" Offset="0"/>
            <GradientStop Color="#60A5FA" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Border Background="Transparent" Padding="10">
        <Border CornerRadius="12" Background="{DynamicResource BackgroundBrush}" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="8" BlurRadius="20" Opacity="0.15"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border x:Name="HeaderBorder" Grid.Row="0" CornerRadius="12,12,0,0" Padding="16" Background="{StaticResource HeaderGradient}">
                    <TextBlock x:Name="TitleBlock" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                </Border>

                <ScrollViewer Grid.Row="1" Margin="16" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="MessageBlock" TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextBrush}"/>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
                    <Button x:Name="OkButton" Width="110" Height="34" Click="OkButton_Click" Background="{StaticResource OkGradient}" Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" Padding="10,6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
