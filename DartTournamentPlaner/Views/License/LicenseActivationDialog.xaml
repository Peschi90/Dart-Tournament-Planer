<Window x:Class="DartTournamentPlaner.Views.License.LicenseActivationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:DartTournamentPlaner.Views.License"
        mc:Ignorable="d" 
        Title="License Activation" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <!-- This XAML is actually not used as the C# class creates the UI programmatically -->
    <!-- But we need a valid XAML structure for compilation -->
    <Grid Background="{DynamicResource MainBackgroundBrush}">
        <Border Background="{DynamicResource DialogInfoGradient}" CornerRadius="12" Padding="16">
            <TextBlock Text="This dialog is created programmatically in code-behind" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="14"
                       Foreground="{DynamicResource TextBrush}"/>
        </Border>
    </Grid>
</Window>