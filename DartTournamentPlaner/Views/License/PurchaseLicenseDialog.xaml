<Window x:Class="DartTournamentPlaner.Views.License.PurchaseLicenseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="License Purchase" Height="750" Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        MinHeight="600" MinWidth="650"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Modern Shadow Effects -->
        <DropShadowEffect x:Key="CardShadow" 
                          Color="Black" 
                          Direction="270" 
                          ShadowDepth="4" 
                          BlurRadius="16" 
                          Opacity="0.08"/>

        <DropShadowEffect x:Key="HeaderShadow" 
                          Color="Black" 
                          Direction="270" 
                          ShadowDepth="2" 
                          BlurRadius="12" 
                          Opacity="0.1"/>

        <!-- Modern Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <!-- Input Field Style -->
        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="2,3"/>
            <Setter Property="Margin" Value="0,2,0,10"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource DialogPrimaryGradient}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#3B82F6" Direction="0" ShadowDepth="0" 
                                                          BlurRadius="8" Opacity="0.25"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,6,0,16"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Template="{DynamicResource ComboBoxToggleButton}"
                                          Grid.Column="2" 
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="12,6,30,6"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}" 
                                     Template="{DynamicResource ComboBoxTextBox}" 
                                     HorizontalAlignment="Left" 
                                     VerticalAlignment="Center" 
                                     Margin="12,6,30,6"
                                     Focusable="True" 
                                     Background="Transparent"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup" 
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource SurfaceBrush}" 
                                            BorderBrush="{DynamicResource BorderBrush}" 
                                            BorderThickness="1" 
                                            CornerRadius="8"
                                            Effect="{StaticResource CardShadow}"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="True">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsGrouping" Value="True"/>
                                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox ToggleButton Template -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="32" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" 
                        Grid.ColumnSpan="2"
                        CornerRadius="8"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="2"/>
                <Border Grid.Column="0"
                        CornerRadius="8,0,0,8"
                        Background="Transparent" 
                        BorderBrush="Transparent"
                        BorderThickness="0"/>
                <Path x:Name="Arrow"
                      Grid.Column="1"     
                      Fill="{DynamicResource SecondaryTextBrush}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#9CA3AF"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource DialogPrimaryGradient}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="Border" Property="Background" Value="#F3F4F6"/>
                    <Setter TargetName="Border" Property="BorderBrush" Value="#D1D5DB"/>
                    <Setter Property="Foreground" Value="#9CA3AF"/>
                    <Setter TargetName="Arrow" Property="Fill" Value="#9CA3AF"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox TextBox Template -->
        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}"/>
        </ControlTemplate>

        <!-- Action Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource DialogPrimaryGradient}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Effect="{StaticResource CardShadow}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource DialogInfoGradient}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#3B82F6" Direction="270" ShadowDepth="0" 
                                                          BlurRadius="20" Opacity="0.4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Effect="{StaticResource CardShadow}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main container with modern design -->
    <Border Background="{DynamicResource MainBackgroundBrush}" 
            CornerRadius="16" 
            Effect="{StaticResource HeaderShadow}"
            Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with modern gradient design -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource DialogInfoGradient}" 
                    CornerRadius="16,16,0,0" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="0,0,0,1" 
                    Padding="32,24"
                    Effect="{StaticResource HeaderShadow}">
                <StackPanel>
                    <TextBlock x:Name="TitleText" Text="License Purchase" 
                               FontSize="28" FontWeight="Bold" 
                               Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="SubtitleText" 
                               Text="Fill out the form below to request a license for Dart Tournament Planner" 
                               FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}"/>
                </StackPanel>
            </Border>

            <!-- Content ScrollViewer with white background -->
            <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" Margin="0">
                <ScrollViewer Padding="32,24" VerticalScrollBarVisibility="Visible" >
                    <StackPanel MaxWidth="650">
                        
                        <!-- Personal Information Card -->
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="PurchasePersonalInformation" Text="Personal Information" FontSize="20" FontWeight="Bold" 
                                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>
                                
                                <!-- Name Fields -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock x:Name="FirstNameLabel" Text="First Name *" 
                                                   FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                        <TextBox x:Name="FirstNameTextBox" Style="{StaticResource InputFieldStyle}"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="2">
                                        <TextBlock x:Name="LastNameLabel" Text="Last Name *" 
                                                   FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                        <TextBox x:Name="LastNameTextBox" Style="{StaticResource InputFieldStyle}"/>
                                    </StackPanel>
                                </Grid>
                                
                                <!-- Email -->
                                <TextBlock x:Name="EmailLabel" Text="Email Address *" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="EmailTextBox" Style="{StaticResource InputFieldStyle}"/>
                                
                                <!-- Company -->
                                <TextBlock x:Name="CompanyLabel" Text="Company / Organization" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="CompanyTextBox" Style="{StaticResource InputFieldStyle}"/>
                            </StackPanel>
                        </Border>

                        <!-- License Requirements Card -->
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="PurchaseLicenseRequirements" Text="License Requirements" FontSize="20" FontWeight="Bold" 
                                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>
                                
                                <!-- License Type -->
                                <TextBlock x:Name="LicenseTypeLabel" Text="License Type *" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                <ComboBox x:Name="LicenseTypeComboBox" Style="{StaticResource ComboBoxStyle}">
                                    <ComboBoxItem Content="Personal License (Single activation)" Tag="personal"/>
                                    <ComboBoxItem Content="Professional License (Up to 5 activations)" Tag="professional"/>
                                    <ComboBoxItem Content="Enterprise License (Up to 10 activations)" Tag="enterprise"/>
                                    <ComboBoxItem Content="Custom License (Contact for Details)" Tag="custom"/>
                                </ComboBox>
                                
                                <!-- Features -->
                                <TextBlock x:Name="FeaturesLabel" Text="Required Features" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,16,0,4"/>
                                
                                <StackPanel x:Name="FeaturesPanel">
                                    <CheckBox x:Name="StatisticsFeature" Content="Advanced Statistics" Margin="0,4"/>
                                    <CheckBox x:Name="HubConnectionFeature" Content="Tournament Hub Connection" Margin="0,4"/>
                                    <CheckBox x:Name="PowerScoringFeature" Content="PowerScoring - Intelligent Player Seeding" Margin="0,4"/>
                                    <CheckBox x:Name="PrintFeature" Content="Enhanced Printing" Margin="0,4"/>
                                    <CheckBox x:Name="TournamentOverviewFeature" Content="Tournament Overview" Margin="0,4"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Additional Information Card -->
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="PurchaseAdditionalInformation" Text="Additional Information" FontSize="20" FontWeight="Bold" 
                                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>
                                
                                <!-- Message -->
                                <TextBlock x:Name="MessageLabel" Text="Message / Special Requirements" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                <TextBox x:Name="MessageTextBox" 
                                         Style="{StaticResource InputFieldStyle}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         Height="100"/>
                                
                                <!-- How did you hear about us -->
                                <TextBlock x:Name="SourceLabel" Text="How did you hear about us?" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                                <ComboBox x:Name="SourceComboBox" Style="{StaticResource ComboBoxStyle}">
                                    <ComboBoxItem Content="Search Engine (Google, Bing, etc.)"/>
                                    <ComboBoxItem Content="Social Media"/>
                                    <ComboBoxItem Content="Word of Mouth"/>
                                    <ComboBoxItem Content="Professional Network"/>
                                    <ComboBoxItem Content="Dart Community / Forum"/>
                                    <ComboBoxItem Content="Other"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <!-- Hardware ID Information Card -->
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="PurchaseSystemInformation" Text="System Information" FontSize="20" FontWeight="Bold" 
                                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>
                                
                                <TextBlock x:Name ="PurchaseHardwareInfoI" Text="Your Hardware ID (automatically included in the request):" 
                                           FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                                
                                <Border Background="{DynamicResource HoverBrush}" 
                                        BorderBrush="{DynamicResource BorderBrush}" 
                                        BorderThickness="1" 
                                        CornerRadius="6" 
                                        Padding="12,8">
                                    <TextBlock x:Name="HardwareIdText" 
                                              FontFamily="Consolas" FontSize="12" 
                                              Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap"/>
                                </Border>
                                
                                <TextBlock x:Name="PurchaseHardwareInfoII" Text="This Hardware ID will be used for license activation and is required for processing your request." 
                                           FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource SurfaceBrush}" 
                    CornerRadius="0,0,16,16"
                    BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="0,1,0,0" 
                    Padding="32,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" 
                            Content="Cancel" 
                            Style="{StaticResource SecondaryButton}" 
                            Click="CancelButton_Click"/>
                    
                    <Button x:Name="SendRequestButton" 
                            Content="Send License Request" 
                            Style="{StaticResource PrimaryButton}" 
                            Click="SendRequestButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>