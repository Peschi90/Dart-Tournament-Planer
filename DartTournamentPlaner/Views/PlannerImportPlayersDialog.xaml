<Window x:Class="DartTournamentPlaner.Views.PlannerImportPlayersDialog"
        x:Name="PlannerImportPlayersDialogRoot"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Planner Players" Height="560" Width="820"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#3B82F6" Offset="0"/>
                        <GradientStop Color="#2563EB" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="Transparent" Margin="10">
        <Border Background="{x:Static SystemColors.WindowBrush}"
                CornerRadius="12"
                BorderBrush="#E2E8F0"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="8" BlurRadius="20" Opacity="0.15"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" CornerRadius="12,12,0,0"
                        Padding="24,18,24,18"
                        Background="{DynamicResource DialogPrimaryGradient}"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👥" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="HeaderTitle" Text="Turnierteilnehmer zuordnen" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1" Margin="24,16,24,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Background="#F9FAFB"
                            BorderBrush="#E5E7EB"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="16">
                        <Grid>
                            <DataGrid x:Name="PlayersDataGrid"
                                      ItemsSource="{Binding Players}"
                                      AutoGenerateColumns="False"
                                      HeadersVisibility="Column"
                                      CanUserAddRows="False"
                                      Margin="0,0,0,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn x:Name="FirstNameColumn" Header="Vorname" Binding="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                    <DataGridTextColumn x:Name="NicknameColumn" Header="Spitzname" Binding="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                    <DataGridTextColumn x:Name="LastNameColumn" Header="Nachname" Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                    <DataGridTextColumn x:Name="EmailColumn" Header="E-Mail" Binding="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                    <DataGridComboBoxColumn x:Name="ClassColumn" Header="Klasse" Width="120"
                                                           SelectedItemBinding="{Binding SelectedClass, UpdateSourceTrigger=PropertyChanged}"
                                                           ItemsSource="{Binding ClassOptions, ElementName=PlannerImportPlayersDialogRoot}"/>
                                    <DataGridTextColumn x:Name="GroupColumn" Header="Gruppe" Binding="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button x:Name="AddRowButton" Content="Zeile hinzufuegen" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="AddRow_Click"/>
                        <Button x:Name="RemoveRowButton" Content="Zeile entfernen" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="RemoveRow_Click"/>
                     </StackPanel>
                 </Grid>

                <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0"
                        Background="#FAFAFA" CornerRadius="0,0,12,12">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                Margin="24,12,24,12">
                        <Button x:Name="CancelButtonBottom" Content="Abbrechen" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0" IsCancel="True" Click="CancelButton_Click"/>
                        <Button x:Name="SaveButton" Content="Nächster Schritt" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="NextStep_Click"/>
                     </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Border>
 </Window>
